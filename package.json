{
  "name": "music-downloader",
  "version": "1.0.0",
  "description": "Music Downloader App with Backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install",
    "build": "cd backend && npm install --only=production"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {},
  "devDependencies": {}
}
