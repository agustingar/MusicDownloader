[phases.setup]
nixPkgs = ['nodejs_18', 'ffmpeg', 'yt-dlp']

[phases.install]
cmds = ['cd backend && npm ci --only=production']

[start]
cmd = 'cd backend && node server.js'

[variables]
NODE_ENV = 'production' 